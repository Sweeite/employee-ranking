# Standard Postgres connection string.
# In production, connect a Vercel Postgres (Neon) database from the
# Storage tab and Vercel sets this automatically.
# For local dev, point it at any Postgres instance.
POSTGRES_URL=postgres://user:pass@localhost:5432/dbname
